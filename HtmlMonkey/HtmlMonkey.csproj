<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup Label="Configuration" Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <OutputType>Library</OutputType>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <ApplicationIcon />
    <OutputTypeEx>library</OutputTypeEx>
    <StartupObject />
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <Description>HtmlMonkey is a lightweight HTML/XML parser written in C#. It allows you to parse an HTML or XML string into a hierarchy of node objects, which can then be traversed or queried using jQuery-like selectors. In addition, the node objects can be modified or even built from scratch using code. Finally, the classes can generate the HTML or XML from the data.</Description>
    <Copyright>Copyright © 2019-2020 SoftCircuits</Copyright>
    <NeutralLanguage>en-US</NeutralLanguage>
    <PackageTags>html-parser xml-parser html xml parse parser crawler spider</PackageTags>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <PackageIconUrl></PackageIconUrl>
    <RepositoryUrl>https://github.com/SoftCircuits/HtmlMonkey</RepositoryUrl>
    <Company>SoftCircuits</Company>
    <Authors>Jonathan Wood</Authors>
    <PackageProjectUrl>https://github.com/SoftCircuits/HtmlMonkey</PackageProjectUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseFile>License.txt</PackageLicenseFile>
    <AssemblyVersion>1.1.0.0</AssemblyVersion>
    <FileVersion>1.1.0.0</FileVersion>
    <Version>1.1.0</Version>
    <PackageReleaseNotes>Reworked code; HtmlCDataNode.Text getter now returns an empty string and the setter does nothing rather than getting or setting the CDATA data (use InnerHtml to get the contents); BREAKING CHANGE: Modified ToString() overrides on node classes to print abbreviated content to avoid bogging down debuggers; Renamed SelectorAttribute to AttributeSelector (old name compiles with obsolete warning); Reworked and improved XML comments.</PackageReleaseNotes>
    <PackageId>SoftCircuits.HtmlMonkey</PackageId>
    <PackageIcon>Logo_64x64.png</PackageIcon>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Backup\**" />
    <EmbeddedResource Remove="Backup\**" />
    <None Remove="Backup\**" />
  </ItemGroup>

  <ItemGroup>
    <None Include="..\License.txt">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
    <None Include="..\Logo_64x64.png">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

</Project>